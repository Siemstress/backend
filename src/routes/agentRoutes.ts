import {Express} from "express";

export class AgentRoutes {
    static async register(app: Express) {

        app.post('/api/agentUpdate', async (req, res) => {

        });

        app.get('/api/installAgent', async (req, res) => {

        });


    }
}
